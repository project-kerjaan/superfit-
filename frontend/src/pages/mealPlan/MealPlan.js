import { useState } from "react";
import { Brand } from "../../components";

const MealPlan = () => {
    return (
        <section className="w-[30vw] min-w-[30vw] mx-auto bg-white min-h-screen">
          <Brand/>
        </section>
    )
}

export default MealPlan;